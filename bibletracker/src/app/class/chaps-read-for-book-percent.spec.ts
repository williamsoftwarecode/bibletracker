import { ChapsReadForBookPercent } from './chaps-read-for-book-percent';

describe('ChapsReadForBookPercent', () => {
  it('should create an instance', () => {
    expect(new ChapsReadForBookPercent()).toBeTruthy();
  });
});

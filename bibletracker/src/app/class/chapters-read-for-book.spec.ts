import { ChaptersReadForBook } from './chapters-read-for-book';

describe('ChaptersReadForBook', () => {
  it('should create an instance', () => {
    expect(new ChaptersReadForBook()).toBeTruthy();
  });
});
